# MCT4043 - Air guitar experiment

## Requirements

The app requires the following libraries to be installed.

    * opencv-python
    * simpleaudio
    * mediapipe
    * numpy

## Running

Become the next Brian May by simply typing `python mocap.py` into your terminal.